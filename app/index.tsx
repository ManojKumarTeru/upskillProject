import { Redirect } from 'expo-router';

export default function RootIndex() {
  // This file is a silent landing spot. 
  // The AuthProvider in app/_layout.tsx handles the actual redirection logic.
  return null;
}
